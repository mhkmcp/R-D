# Milestones, tests, reproducibility (SPEC §11, §12)

## Milestones (§12)

| # | Milestone | Exit criterion |
|---|---|---|
| **M-0** | Throughput calibration (**gate**) | Measured samples/s for M1/M2/M3 on MPS and CPU, FP32 and INT8, hooks on/off; §4.2 grid sizes confirmed or revised **before any sweep**; §3.3 reinvestment sized against the measurement |
| M-1 | Models trained, accuracy verified | `manifest.json` within 1 pt: ResNet-20 91.25 %, VGG-11-BN ≈ 92 % (pinned source). Disjointness + label-agreement tests pass. Optional one-shot Kaggle submission |
| **M-1b** | L1 attacker validated (**gate**) | BFA flip-budget-vs-accuracy curve on INT8 ResNet-20 consistent with published; discrepancy explained or fixed **before** generating the fault population |
| M-2 | Injection engine + taxonomy | Bit-flip, buffer-checksum, in-place-restore tests pass; taxonomy distribution plotted for M2 and M3; **Track S share ≥ ~15 % (hard gate)** |
| M-3 | Tap/feature pipeline | Features for M1–M3, default + extended; tap-budget, block-population, determinism tests pass |
| M-4 | D1 + D2 on M1 — **sanity only, no thesis number** | Calibrated FPR within 0.3×–3× nominal; plausible TPR@1%FPR on both tracks |
| M-5 | Full evaluation M2/M3, FP32 + INT8, all baselines | Headline tables generated by `make reproduce`; precision ablation (§9.6(4)) done here |
| M-6 | Overhead study | TPR-vs-latency Pareto front for K sweep, CPU and MPS, FP32 and INT8 |
| M-7 | Ablations + H4 | §9.6 items 1–6 and 8, CIFAR-10-C per type and per family, Block-D downgrade, M2-vs-M3 propagation |
| M-8 | L2 adaptive attacker | Success-rate/budget table with vs without monitor |
| M-9 | Writing, threats to validity | Draft complete |

M-1…M-5 settle H1/H2 (minimum viable thesis). M-6/M-7 settle H3; M-7 settles H4; M-8 raises the work
above an empirical study.

**Current code state (check before relying on it):** `fidnn bench m0` exists
(`src/fidnn/bench/throughput.py`, `configs/bench/m0.yaml`); `extract/inject/fit/calibrate/eval/report`
are stubs that name their milestone.

## Required tests (§11.4)

- Bit-flip round trip (FP32 and INT8), bit-exact.
- Known value: FP32 bit 30 of a known float → expected value.
- In-place restore after a simulated mid-probe exception: `state_dict` checksum (params **and
  buffers**) equals clean.
- CIFAR-10 source disjointness (pixel hash) and label agreement.
- No ZCA / full-covariance transform in the input pipeline.
- **C1 grep guard** over `src/` (the file lives at `tests/*c1*` — the only code allowed to spell out
  banned names).
- Hook coverage: each tap fires exactly once per forward; removing hooks restores baseline latency.
- Tap budget ≥ 5 per model, from the registry.
- Feature-block population: D-conv non-zero on all conv taps; D-dense on synthetic `(N, D)` tensors.
- Leakage guard (§7) incl. channel stats fitted on `clean_fit` only.
- Feature determinism: same input ⇒ same vector.
- Overhead harness: monitor-off path indistinguishable from the unmodified model.

## Reproducibility (§11.3)

- Every artifact has a **sidecar JSON**: git commit, config hash, seed, library versions, **device**,
  and for CIFAR runs the data hashes + disjointness result.
- **Reported experiments and all §9.4 overhead numbers run on CPU** (determinism). MPS is exploratory
  only; the sidecar's device field makes an MPS-produced number detectable.
- `make reproduce` regenerates headline tables from cached features.
- Analysis logic lives in `src/`; notebooks only read `artifacts/results/*.parquet` and draw figures.
- `artifacts/**` is write-protected from direct edits (settings `deny`); artifacts are produced by the
  CLI, never hand-edited.

## Commands

```bash
uv run pytest                           # all tests
uv run ruff check src tests
uv run python -m fidnn bench m0 --quick # M-0, ~minutes
uv run python -m fidnn bench m0         # M-0, full
```
